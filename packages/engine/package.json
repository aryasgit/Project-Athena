{
  "name": "@athena/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Athena Organization Engine — deterministic, seeded, serializable simulation core. No React, no DOM, no I/O.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "5.7.3"
  }
}
